import styled from 'styled-components';
import { Container } from '../styled/containers';

export const CardsContainer = styled(Container)`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;

  /* TODO: Add styling for child CardWithIcon elements */
`;
