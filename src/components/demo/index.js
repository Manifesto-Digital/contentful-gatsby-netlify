import React from 'react'

// Styles
import { Wrapper } from './styles'

const ComponentDemo = ({ children }) => (
  <Wrapper>
    <h1>DEMO</h1>
    {children}
  </Wrapper>
)

export default ComponentDemo
